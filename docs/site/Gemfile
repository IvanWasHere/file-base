source "https://rubygems.org"

gem "jekyll", "~> 4.4"

# `jekyll serve` on Ruby 3+ needs a web server gem, which Ruby stopped bundling.
gem "webrick", "~> 1.9"

# Link and image checking for the built site (CI's "Check" step).
group :test do
  gem "html-proofer", "~> 5.0"
end
